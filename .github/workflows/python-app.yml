name: Python application

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Sprawdzenie kodu
        uses: actions/checkout@v4

      - name: Konfiguracja Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            pyproject.toml

      - name: Instalacja zależności
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[mcp]" flake8 pytest

      - name: Krytyczna kontrola składni flake8
        run: |
          flake8 perplexity perplexity_async packages apps/backend tests \
            --count --select=E9,F63,F7 --show-source --statistics

      - name: Raport jakości flake8
        run: |
          # Repo zawiera starszy kod i warstwę koncepcyjną. Problemy jakościowe,
          # w tym F82, są raportowane, ale nie blokują PR-a; składnia i testy nadal blokują.
          flake8 perplexity perplexity_async packages apps/backend tests \
            --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Kompilacja modułów Python
        run: python -m compileall -q perplexity perplexity_async packages apps/backend

      - name: Testy pytest
        run: pytest -q
