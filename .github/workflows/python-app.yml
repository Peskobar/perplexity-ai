name: Python application

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Sprawdzenie kodu
        uses: actions/checkout@v4

      - name: Konfiguracja Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            pyproject.toml

      - name: Instalacja zależności
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[mcp]" flake8 pytest

      - name: Blokująca kontrola składni biblioteki
        run: python -m compileall -q perplexity perplexity_async tests

      - name: Raport jakości flake8
        run: |
          # Starszy szkielet aplikacji jest raportowany, ale nie blokuje zmian
          # w bibliotece Perplexity/MCP. Błędy składni biblioteki i testy blokują PR.
          flake8 perplexity perplexity_async packages apps/backend tests \
            --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Testy pytest
        run: pytest -q
